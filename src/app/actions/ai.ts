


// export const parseFiles = (files: File[]):Promise<any> => {
//     return new Promise((resolve, reject) => {});
//     }
    // send
// export const generateProduct = (formData: FormData): Promise<any> => {
//     const files:any = formData.getAll("file");

//     // parse lkist of files and avise which to exclude
//     const { excludeFiles, includeFiles }:any = parseFiles(files);

//     // generate product
//     return generateProductAction(includeFiles, excludeFiles);
// }
export {}
